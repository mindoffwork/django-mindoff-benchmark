# Pinned so the published benchmark numbers are reproducible.
#
# This benchmark currently tracks: django-mindoff 0.6.0
# When a new django-mindoff release lands, bump the pin below, re-run the
# benchmark, and refresh the artifacts in benchmarks/ (see the README).
#
# django-mindoff ships its runtime stack (DRF, Polars, ConnectorX, python-decouple,
# Dramatiq/Redis, pytest helpers, ...) under the "internal" extra. Installing the
# bare package would leave this project unable to import its own settings, so pull
# the extra explicitly.
django-mindoff[internal]==0.6.0

# Example-only extras: the benchmark renders PNG charts (matplotlib) and samples
# peak RSS in a worker process (psutil). These are not part of the framework.
matplotlib~=3.11
psutil~=7.2
